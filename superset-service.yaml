apiVersion: v1
kind: Service
metadata:
  labels:
    app: superset
  name: superset
spec:
  type: NodePort
  ports:
  - name: "superset-service"
    port: 5000
    targetPort: 8088
    nodePort: 31000
  selector:
    app: superset
  
